VITE_DEV_HOST=0.0.0.0
VITE_DEV_PORT=5173
VITE_BACKEND_URL=http://localhost:8000

# Optional: bypass Vite proxy and call the API directly.
# VITE_API_BASE=http://localhost:8000

# Keep VITE_DEV_PORT aligned with ../.env -> FRONTEND_PORT when using Docker Compose.
